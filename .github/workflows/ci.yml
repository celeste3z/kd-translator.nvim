name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.actor }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        neovim_version: [v0.12.0, nightly]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: ${{ matrix.neovim_version }}
      - run: nvim --version
      - name: Install mini.test & mini.doc
        run: |
          git clone --depth 1 https://github.com/nvim-mini/mini.test \
            ~/.local/share/nvim/site/pack/core/opt/mini.test
          git clone --depth 1 https://github.com/nvim-mini/mini.doc \
            ~/.local/share/nvim/site/pack/core/opt/mini.doc
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - run: make test

  lint-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: v0.12.0
      - name: Install mini.test & mini.doc
        run: |
          git clone --depth 1 https://github.com/nvim-mini/mini.test \
            ~/.local/share/nvim/site/pack/core/opt/mini.test
          git clone --depth 1 https://github.com/nvim-mini/mini.doc \
            ~/.local/share/nvim/site/pack/core/opt/mini.doc
      - run: make --silent documentation
      - run: if [[ -n $(git status -s) ]]; then exit 1; fi
